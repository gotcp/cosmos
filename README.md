# Cosmos is an HTTP/HTTPS service written in Golang, it's like Nginx and Apache.
--------------
* Use EPOLL network
* Use thread pool
* Use buffer pool
* Support SSL/TLS(OpenSSL)

Running Cosmos
--------------
    % go run src/core/cosmos/cosmos.go